const moduleName = 'ibuilder';
const moduleVersion = 'v1';
const urlBase = `/${moduleName}/${moduleVersion}`


export default {
  urlBase: urlBase,
  version: moduleVersion,
  blocks: `${urlBase}/blocks`,
  templates: `${urlBase}/templates`,
}
